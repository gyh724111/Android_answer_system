<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.yonghuige.testjson">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".avtivitys.TC_Search" />
        <activity android:name=".avtivitys.TC_Detail" />
        <activity
            android:name=".avtivitys.LoginActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".avtivitys.All_RP_Search" />
        <activity android:name=".avtivitys.GuideActivity" />
        <activity
            android:name=".avtivitys.OrderActivity"
            android:label="@string/title_activity_order"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".avtivitys.MyOrdersActivity" />
        <activity android:name=".avtivitys.MyOrderedActivity" />
        <activity android:name=".avtivitys.MyOrdered_Detail"></activity>
    </application>

</manifest>